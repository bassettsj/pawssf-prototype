extends layout

block header
  include author
  h1= page.title

block content
  article.article
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a.btn(href=contents.index.url) « Back to docs
